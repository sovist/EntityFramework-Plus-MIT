name: Publish to NuGet

# Pushing a tag `mit/<version>` on master-MIT is the release: this builds, tests, packs the library as
# `<version>` and pushes it to nuget.org. Prereleases work the same way (`mit/10.105.8.1-preview.1`).
# `workflow_dispatch` re-runs a release for an existing tag.
on:
  push:
    tags: [ "mit/*" ]
  workflow_dispatch:
    inputs:
      version:
        description: "Package version to publish (must match an existing mit/<version> tag)"
        required: true

env:
  DOTNET_NOLOGO: true
  DOTNET_CLI_TELEMETRY_OPTOUT: true

jobs:
  publish:
    runs-on: windows-latest
    timeout-minutes: 30
    # Trusted Publishing: nuget.org has a policy bound to this repository and workflow file; NuGet/login
    # exchanges the job's OIDC token for a short-lived API key, so no secret is stored anywhere.
    permissions:
      contents: read
      id-token: write

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Resolve version
        shell: bash
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            VERSION="${{ inputs.version }}"
          else
            VERSION="${GITHUB_REF_NAME#mit/}"
          fi
          # The package version mirrors the upstream tag the build is made from, which the csproj records
          # as <Version>. A release tag may only be that version or a prerelease of it.
          CSPROJ_VERSION=$(sed -n 's/.*<Version>\(.*\)<\/Version>.*/\1/p' src/Z.EntityFramework.Plus.EFCore10x.NET10/Z.EntityFramework.Plus.EFCore10x.NET10.csproj)
          case "$VERSION" in
            "$CSPROJ_VERSION" | "$CSPROJ_VERSION"-*) ;;
            *) echo "::error::Version '$VERSION' does not match the csproj <Version> '$CSPROJ_VERSION' (the upstream tag this build is made from)."; exit 1 ;;
          esac
          echo "VERSION=$VERSION" >> "$GITHUB_ENV"
          echo "Publishing EntityFramework.Plus.EFCore.MIT $VERSION"

      - name: Setup .NET
        uses: actions/setup-dotnet@v6
        with:
          dotnet-version: 10.0.x

      - name: Install SQL Server
        uses: Potatoqualitee/mssqlsuite@v2
        with:
          install: sqlengine
          version: 2022

      - name: Build
        run: dotnet build src/EntityFramework.Plus.EFCore.MIT.slnf --configuration Release -p:Version=${{ env.VERSION }}

      - name: Test
        run: dotnet test src/EntityFramework.Plus.EFCore.MIT.slnf --configuration Release --no-build --verbosity normal

      - name: Pack
        run: dotnet pack src/Z.EntityFramework.Plus.EFCore10x.NET10/Z.EntityFramework.Plus.EFCore10x.NET10.csproj --configuration Release --no-build -p:Version=${{ env.VERSION }} --output artifacts

      - name: Upload package
        uses: actions/upload-artifact@v7
        with:
          name: nupkg-${{ env.VERSION }}
          path: artifacts/*.nupkg

      - name: NuGet login
        id: nuget-login
        uses: NuGet/login@v1
        with:
          user: oleksandr.semeniuk

      - name: Push
        run: dotnet nuget push artifacts/EntityFramework.Plus.EFCore.MIT.${{ env.VERSION }}.nupkg --source https://api.nuget.org/v3/index.json --api-key ${{ steps.nuget-login.outputs.NUGET_API_KEY }} --skip-duplicate
